import numpy as np
import matplotlib.pyplot as plt
from sklearn.model_selection import train_test_split #Import split method from Scikit-learn, this will allow us to split data sets into testing and training sets

